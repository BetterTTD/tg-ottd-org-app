import { FC } from 'react';

const Footer : FC = () => (
    <footer className="mt-auto text-center text-white-50">
        <p>OpenTTD: TeamGame community.</p>
    </footer>
);

export default Footer;